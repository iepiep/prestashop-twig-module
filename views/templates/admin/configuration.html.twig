{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    <div class="card">
        <h3 class="card-header">
            <i class="material-icons">settings</i> {{ 'Configuration'|trans({}, 'Modules.Dimsymfony.Admin') }}
        </h3>
        <div class="card-body">
            {{ form_start(form) }}
                {{ form_row(form.google_api_key) }}
                <button type="submit" class="btn btn-primary">
                    {{ 'Save'|trans({}, 'Admin.Actions') }}
                </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
